---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photos
  namespace: nextcloud
spec:
  storageClassName: shared-hdd
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 8T
