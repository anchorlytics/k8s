apiVersion: helm.cattle.io/v1
kind: HelmChart
metadata:
  name: metallb
  namespace: kube-system
spec:
  chart: stable/metallb
  version: 0.12.0
  targetNamespace: kube-system
  valuesContent: |-
    configInline:
      address-pools:
        - name: LAN
          protocol: layer2
          addresses:
            - 10.90.4.96/26
